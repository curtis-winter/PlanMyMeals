services:
  app:
    build: .
    network_mode: host
    ports:
      - "3112:3000"
    environment:
      - NODE_ENV=development
    volumes:
      - .:/app
      - /app/node_modules
      - meals_data:/app/data

volumes:
  meals_data:
