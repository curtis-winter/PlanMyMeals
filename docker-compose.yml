services:
  app:
    build: .
    ports:
      - "3112:3112"
    extra_hosts:
      - "host.docker.internal:host-gateway"
    volumes:
      - ./build-number.json:/app/build-number.json:ro
      - meals_data:/app/data

volumes:
  meals_data:
