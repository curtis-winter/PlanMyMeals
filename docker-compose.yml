services:
  app:
    build: .
    network_mode: host
    environment:
      - NODE_ENV=development
    volumes:
      - .:/app
      - /app/node_modules
      - meals_data:/app/data

volumes:
  meals_data:
